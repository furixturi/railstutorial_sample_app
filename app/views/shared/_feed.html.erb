<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %><!-- since each feed item is of class Micropost, Rails will search a partial in views/microposts/_micropost.html.erb -->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>